name: "Test docker-compose-web-modeler.yaml"
on:
  push:
    branches:
      - "**"
jobs:
  test_modeler_yaml:
    uses: ./.github/workflows/template-deploy.yaml
    secrets: inherit
    with:
      compose_args: "--profile modeler"
      run_e2e_tests: true
